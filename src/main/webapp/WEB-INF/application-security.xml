<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xsi:schemaLocation="
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

    <http use-expressions="true" auto-config="true">
        <intercept-url pattern="/" access="permitAll"/>
        <intercept-url pattern="/logout*" access="permitAll"/>
        <intercept-url pattern="/login" access="permitAll"/>
        <intercept-url pattern="/register" access="permitAll"/>
        <intercept-url pattern="/projects**" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/home**" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/dashboard**" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/reports**" access="hasRole('ROLE_USER')"/>
        <intercept-url pattern="/statistics**" access="hasRole('ROLE_USER')"/>

        <access-denied-handler error-page="/login?error"/>

        <!--        <session-management invalid-session-url="/login" session-fixation-protection="none">-->
        <!--            <concurrency-control max-sessions="1" error-if-maximum-exceeded="true"-->
        <!--                                 expired-url="/login" session-registry-alias="sessionRegistry"/>-->
        <!--        </session-management>-->
        <form-login login-page="/login" default-target-url="/first" authentication-failure-url="/login?error"
                    username-parameter="usermail" password-parameter="password"/>

        <logout invalidate-session="true" logout-success-url="/logout?logout" delete-cookies="JSESSIONID"/>
        <csrf/>
        <http-basic/>
    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider user-service-ref="userDetailsServiceImpl">
            <password-encoder ref="encoder"/>
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="userDetailsServiceImpl" class="vadim.volin.services.impl.UserDetailsServiceImpl"/>

    <beans:bean id="encoder"
                class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder">
        <beans:constructor-arg name="strength" value="11"/>
    </beans:bean>
</beans:beans>